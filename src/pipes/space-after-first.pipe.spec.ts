import { SpaceAfterFirstPipe } from './space-after-first.pipe';

describe('SpaceAfterFirstPipe', () => {
  it('create an instance', () => {
    const pipe = new SpaceAfterFirstPipe();
    expect(pipe).toBeTruthy();
  });
});
